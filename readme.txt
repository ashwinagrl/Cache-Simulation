ReadMe:
I have correctly implemented all the 4 parts.

Instructions to run the program:

Please make sure you have g++ installed:
-To install g++:
	sudo apt install g++

-To run the program, run the following commands on the terminal:
	g++ source_code.cpp
	./a.out
	
- Make sure your cache configuration and Access sequence are in the cache.config and cache.access files resplectively. And they are in the same directory as the source_code.cpp file.